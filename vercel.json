{
  "version": 2,
  "builds": [
    {
      "src": "frontend/",
      "use": "@vercel/node",
      "config": {
        "maxLambdaSize": "2048mb",
        "buildCommand": "npm install && npm run build"
      }
    },
    {
      "src": "backend/",
      "use": "@vercel/node"
    }
  ]
}
